# Examples

This folder contains sanitized examples that are safe to publish.

## `framework-template/`

A sanitized generated `study` workspace. It demonstrates the shared `.framework/`, `systems/`, and `knowledge/` base plus the `references/` and `analyses/` directories that the `study` archetype adds.

It intentionally does not include real frozen external repositories, private notes, runtime logs, generated release artifacts, or secrets.
