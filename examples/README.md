# Examples

This folder contains sanitized examples that are safe to publish.

## `framework-template/`

A generated MetaSystem framework workspace. It demonstrates the managed `.framework/` layer, reference intake folders, analysis folders, system implementation area, iteration area, knowledge area, and release area.

It intentionally does not include real frozen external repositories, private notes, runtime logs, generated release artifacts, or secrets.
