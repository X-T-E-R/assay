# Packages

This monorepo currently contains one package:

- `metasystem-framework-cli/` — the Python CLI used to create, check, update, migrate, and operate MetaSystem framework workspaces.

Keep package-specific build metadata inside the package directory. Keep repository-level checks and documentation at the monorepo root.
